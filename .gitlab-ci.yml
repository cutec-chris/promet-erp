before_script:
  - git submodule update --init --recursive
build:
  script:
    - bash build.sh all
    
build-windows:
  script: git-bash build.sh all
  tags:
    - Windows*
build-linux-i386:
  script: bash build.sh all
  tags:
    - Linux,i386
